rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Solo usuarios autenticados (incluyendo anónimos) pueden leer y escribir
    // El bot usa Admin SDK y bypass estas reglas automáticamente
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
